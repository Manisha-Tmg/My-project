import React from "react";

const Input = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        justifyContent: "center",
        alignItems: "center",
        display: "inline-flex",
      }}
    >
      <div
        style={{
          width: 380,
          height: 50,
          background: "white",
          borderRadius: 10,
        }}
      />
    </div>
  );
};

export default Input;
